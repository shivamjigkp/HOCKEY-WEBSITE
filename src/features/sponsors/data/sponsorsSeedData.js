/**
 * ⚠️ SUPERSEDED as of Phase 10 — sponsors now live in Supabase
 * (see supabase/schema_phase10.sql, src/services/sponsors.js).
 * Nothing imports this file anymore; kept only for history.
 *
 * ⚠️ PLACEHOLDER — no sponsors are confirmed yet. This array is
 * intentionally empty so the SponsorsStrip component renders nothing
 * until real sponsors are signed and added here.
 *
 * `src/services/sponsors.js` is the only file that should import this.
 * Shape once populated: { id, name, logo, website }
 */

export const SPONSORS_SEED = [];
